Create application. 
User enters some number of text lines ( stop reading text when user enters empty line). 
Application returns: 
1. Number of unique words; 
2. Sorted list of all unique words; 
3. Word count. Occurence number of each word in the text(e.g. text "a s a" -> a-2 s-1). Use group collector;
4. Occurence number of each symbol except upper case characters